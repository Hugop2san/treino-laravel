# Calculos de analises

- Produto mais vendido: contar quantas linhas existem na tabela `sales` para cada `product_id`.
- Produto mais caro: consultar a tabela `products` e localizar o maior valor do campo `preco`.
- Persistencia esperada: salvar os nomes dos produtos na tabela `analyses`.
